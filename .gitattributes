# Weights are stored as regular git files, sharded below GitHub's 100MB limit.
# Do not track them with Git LFS — clones without `git lfs pull` would get
# an unusable pointer instead of a real checkpoint.
